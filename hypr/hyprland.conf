autogenerated = 0

monitor=HDMI-A-1, 1920x1080@120, 0x0, 1

$mainMod = SUPER

exec-once = hyprlock
exec-once = swaybg -i /home/watashi/Pictures/wallpapers/blackforest.jpg
exec-once = waybar
exec-once = bash -c "for i in {1..5}; do hyprctl dispatch workspace $i; done &&  hyprctl >

input {
        kb_layout = us,ru
        kb_options = grp:alt_shift_toggle
        follow_mouse = 1
}

general {
        gaps_in = 5
        gaps_out = 10
        border_size = 2
        col.inactive_border = rgba(595959aa)
        layout = dwindle
}

decoration {
    rounding = 16

    active_opacity = 0.80
    inactive_opacity = 0.80
    fullscreen_opacity = 1.0

    blur {
        enabled = true
        size = 7
        passes = 0
        new_optimizations = true
        ignore_opacity = true
        xray = false
    }

    shadow {
        enabled = true
        range = 20
        render_power = 3  
        color = rgba(00000055)
        offset = 0 5
    }

}

animations {
    enabled = yes
    bezier = md3_decel, 0.05, 0.7, 0.1, 1

    animation = windows, 1, 5, md3_decel, popin 95%
    animation = windowsOut, 1, 5, md3_decel, popin 95%
    animation = workspaces, 1, 6, md3_decel, slide
    animation = layers, 1, 5, md3_decel, slide
    animation = fade, 1, 5, md3_decel
}

bind = $mainMod, T, exec, kitty
bind = $mainMod, W, exec, zen-browser
bind = $mainMod, Q, killactive,
bind = $mainMod, R, exec, rofi -show drun
bind = $mainMod, E, exec, Thunar
bind = SUPER_SHIFT, S, exec, grim -g "$(slurp)" - | wl-copy
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bindm = $mainMod, mouse:273, resizewwindow

workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true
workspace = 4, persistent:true

env = GTK_THEME,adw-gtk3-dark
env = QT_STYLE_OVERRIDE,Adwaita-Dark

env = XCURSOR_THEME,Bibata-Modern-Classic
env = XCURSOR_SIZE,24
env = HYPRCURSOR_THEME,Bibata-Modern-Classic
env = HYPRCURSOR_SIZE,24

exec-once = hyprctl setcursor Bibata-Modern-Classic 24
